# Acceptance status — 2026-09-25

This deployment is **0.1.0-dev**, not a completed 0.1.0 release. Do not describe it as identical to Wateroffice. No model-performance evaluation has been rerun.

## Verified

- Full official HYDAT 2026-07-17 database, not a benchmark-specific subset: 8,057 station records, 8,001 stations with daily Flow/Level. Original monthly flags, daily values, zeros and quality symbols retained.
- Complete website realtime station snapshot: 2,248 unique IDs, including stations omitted from the smaller Datamart list. Seven-day observations total 4,325,421; 67 official empty responses are distinguished from unarchived resources.
- Complete national river reference CSV and 86 linked HTML pages. HTML pagination and ordering use the source sequence, not station numbers, coordinates or drainage areas.
- Independent five-task recomputation: 1,728 monthly comparisons with official availability evidence, 13 additional daily API checks, 32 preselected non-answer exploration queries, 41 restricted-browser candidate/matrix checks.
- Historical daily downloads compared against complete original Quick station exports: CSV 3,443 rows, DDF and JDF 39,944 rows each. Daily and monthly table summaries independently compared.
- Ten CG/GO records checked across 40 task/format records. 001/002/004 corrections applied to local/server task files with originals preserved. 003/005 unchanged after verification.
- Internal evaluation network, external TCP refusal, nonroot/readonly/capability restrictions, Chromium sandbox, exact-host URL mapping and private-path denial tested. Browser reset removes session lists and downloads; stale download IDs are inaccessible; controlled chunked reads work.

## Outstanding release requirements

1. **Appearance:** nine page pairs captured at 1440×1000 in the same Chromium. Fonts and reference-index HTML are localized. This is not full visual acceptance: the original map data request remained pending; replaying the exact archived official station response into the live original renderer produced a usable, explicitly annotated reference. Eight non-map page pairs have now been recaptured with explicit font readiness; the inspected historical graph viewport matches closely. Earlier timeout captures are not acceptance evidence. All required popups, download states and responsive transitions have not been exhaustively compared. Current interface uses release 60; old task archives use release 58. The 79 older pages have been audited for 485 selected associated CSS/JS files: application/print CSS, jQuery, application JS, realtime modifier and localized theme CSS match; translations differ only by two additional French labels. This does not constitute page-by-page HTML or visual equivalence.
2. **Map:** Leaflet plus genuine NRCan Toporama tiles provides national overview and five river-region detail areas. It differs from original Google Maps, including tiles and controls (the recovered original reference shows unclustered markers). All 11 official basin polygon files have been acquired and deployed (about 42 MiB compressed). Original-style popups, polygon display and opacity passed browser tests. Dragging and delayed-response cancellation also passed on the deployed server. Satellite/terrain views are not implemented. High zoom outside captured areas cannot be treated as original-site coverage.
3. **Search:** operation-schedule historical filtering now replays the complete official national search membership (with duplicate rows), checked in 15 combinations. Pairing it with a specific Flow/Level parameter produced HTTP 500 on the original and remains explicitly unavailable. Realtime parameter/regulation/operation/agency memberships match 12 official national searches and eight independent combined searches. Area and coordinate search now pass 17 independent official queries plus five invalid-input cases, including exact US-station coordinate boundaries. Real-time details use complete official metadata for all 2,248 stations; source timestamps are separate from the HYDAT release. Supported historical record-length/year/parameter/area/regulation queries do not establish all-filter equivalence.
4. **Reports:** historical comparison statistics now pass 13 archived official cases; statistics controls/curves, mode round trips and station-remarks navigation passed the deployed sandboxed browser tests. See `validation/statistics-method.md`. Datum conversion pages and the illustrated FAQ now cover the full 8,082-station HYDAT/realtime union. All 8,083 rendered page bodies match independent parsing of official HTML; 26 HTTP samples include two legacy alphanumeric station IDs. Real-time daily means are now deployed from official products 3/6: 29,978 observations, with 42 stations having no published daily parameter. CSV/TXT/XML for both parameters match seven complete original days; the partial last day has a separate per-station acquisition timestamp. Graph/table mixed-frequency alignment passed deployed browser checks. Historical daily CSV/DDF/JDF, monthly CSV/DDF, annual daily extremes, instantaneous extremes and remarks are now implemented; five added formats passed row-for-row original-site and deployed-endpoint checks (validation/extra-downloads.json and extra-download-endpoints.json).
5. **Realtime fidelity:** GeoMet supplies observed values and qualifier symbols but not original report Approval/Grade fields. These are blank or marked unavailable, not fabricated. Snapshot first/last calendar dates are partial. Watch-list trend symbols and displayed values now come from a separate, complete national published summary snapshot (13:35–13:41 UTC), including explicit Not Available entries. Five-station pagination and the 50-station limit passed deployed UI/API checks. Month-long raw observations remain outside the seven-day archive. Quick-list editing now uses the captured original parameter table, disabled existing selections, additive parameter updates, individual-parameter deletion confirmation, list-deletion confirmation and rename form. Original route redirects are retained; insertion order is preserved. Simultaneous unit water level/discharge uses the original combined dual-axis graph; daily means remain separate. Three additional original/offline view pairs were captured and inspected; the coverage footer remains an explicit local addition. These samples do not establish every possible state.
6. **Interaction matrix:** full back/forward, two-tab, rapid changes, cross-pagination selection and every original control have not all passed end-to-end acceptance. All 29 targeted author-browser workflows subsequently passed on the deployed server, with no page JavaScript errors; preserved earlier failures include tunnel timeouts, a missing realtime series field and an asynchronous-map test timing issue. See `validation/workflows/ten-workflows-passed.json` and the latest `results.json`; these do not establish exhaustive interaction equivalence. The explicit WebSocket runtime test exposed a synchronous Playwright callback deadlock. The bridge now uses the public async API; internal/external WebSockets were closed before any upgrade reached the fixture, both before and after reset. The deployed async bridge then passed all 41 old-task browser checks and download/reset checks.
7. **Hugging Face publication:** no publication credentials were present on either host. Complete prepared files are in `author/huggingface-proposed`; the remote dataset was not updated. Local and server updates do not imply remote publication.

## Operational distinction

Base `compose.yaml` is the internal evaluation network and was tested as such. `compose.preview.yaml` adds a web preview network and loopback port; it is not the same network-isolation configuration. Browser navigation is still restricted. Existing four services are independent.

Author evidence, task answers and backups are outside image build contexts and public mounts. Development results must not be used to claim final release acceptance. The remaining work above is required to meet the original implementation plan; it has not been dropped from scope.

## Latest continuation

SSH authentication recovered after loading the existing key from the workstation keychain. Production web image 085a50c55292 and browser image fc7612134e37 are development builds. The server has 10.541 GiB free after syncing the final evidence files; every additional transfer/build must recheck the 10 GiB minimum. No 0.1.0 release or HF publication has occurred.

Annual extreme graph/table modes passed eight original graph comparisons, two complete table comparisons and a deployed browser mode-switch test. Historical statistics and remarks passed deployed checks. Daily means have a separate acquisition timestamp: partial September 25 values must not be presented as sharing the 09:40 UTC unit-value cutoff.

Map downloads now reuse the original formatter and export the full filtered set. Historical (11 columns) and realtime (18 columns) CSVs match the original renderer byte for byte when supplied the same official snapshot. An explicit local difference is retained for empty filters: a header-only export, instead of the original JavaScript fallback to all stations. A malformed original station-name HTML pattern was repaired without changing its intended allowed character set.

Realtime graph requests now discard stale responses, clear obsolete plots on an HTTP error and recover on a subsequent valid query. Both local and deployed adversarial delayed-response tests pass (`validation/realtime-race-remote.json`). A local URL-state adapter keeps graph/table/download parameters, refresh, copied URLs and history consistent; the original page does not push these states into browser history, so this is an explicit local adaptation rather than a claim of original behavior.

Datum capture covers 8,082 national station IDs, including official legacy IDs containing letters in the final three positions. The archive does not invent conversion factors for pages publishing none. Quick-list verification includes original form rows/disabled states, no-op empty submissions and removal of only the selected parameter. Source pages and anonymous author cookies remain outside public mounts.

Final deployment checks for web 085a50c55292: all 29 browser workflows passed with no page JavaScript errors; all 8,083 datum page bodies match archived originals, 26 deployed HTTP samples and four invalid inputs passed; five quick-list original/behavior comparisons passed against the deployed server. The six unit checks passed. Full original-map appearance remains unaccepted, unit Approval/Grade metadata remain unavailable, and HF publication remains pending credentials.
