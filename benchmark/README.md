# Author-side regression inputs

This directory contains CG/GO tasks and expected outputs for deterministic workflow checks. Keep it outside website images and runtime mounts.

Use the scripts under `tests/workflows/` to collect normal webpage/download results and compare them against the task definitions. Report browser workflow checks separately from autonomous model experiments.
