# Author-side regression tasks

These 34 current CG/GO records are copied without changing task IDs, instructions, rubrics or answers from the existing SGR-BENCH task corpus. They are evaluation inputs and expected outputs, **not website runtime data**. No runtime image copies or mounts this directory.

Their presence does not assert that every packaged environment is accepted. See docs/STATUS.md and per-site verification. Wateroffice remains development status; NOAA and WONDER redistribution limitations still apply. Existing model results are not recomputed or updated by this release.
